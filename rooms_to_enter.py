import random
import math
from functions import Warrior
mcharacter = Warrior

class SkeletonWarrior():

    def __init__(self, health, mana, strength, magic, sense, dexerity, barriers, money, name):
        self.level = level
        self.health = round((e ** (level * 0.065) * 25) random.uniform(0.6, 0.72),0)
        self.mana = 0
        self.strength = round((e ** (level * 0.07) * 5) random.uniform(0.5, 1.6),0)
        self.magic = 0
        self.sense = round((e ** (level * 0.16) * 2) random.uniform(0.3, 1.25),0)
        self.dexerity = dexerity
        self.barriers = 0
        self.money =
        self.name = 'skeleton'

level = 1
def battle():
